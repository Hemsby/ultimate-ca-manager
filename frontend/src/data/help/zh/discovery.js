export default {
  helpContent: {
    title: '证书发现',
    subtitle: '查找网络中的 TLS 证书',
    overview: '扫描您的网络以查找部署在服务器和端点上的 TLS 证书，并将它们与您管理的 PKI 清单进行匹配。定位未跟踪的证书、检测变化，并监控 UCM 管控之外即将过期的证书。',
    sections: [
      {
        title: '标签页',
        items: [
          { label: '已发现', text: '扫描找到的所有证书，包含状态、到期时间和端点详情' },
          { label: '配置文件', text: '已保存的扫描配置 — 目标、端口、计划、通知' },
          { label: '历史', text: '过去的扫描运行记录，包含持续时间、扫描的目标和找到的证书' },
        ]
      },
      {
        title: '扫描',
        items: [
          { label: '快速扫描', text: '无需保存配置文件的临时扫描 — 输入目标和端口，结果实时呈现' },
          { label: '目标', text: '每行一个：主机名、IP、CIDR 子网（192.168.1.0/24）或 host:port（10.0.0.1:8443）' },
          { label: '端口', text: '逗号分隔的 TCP 端口（如 443, 8443, 636），或使用常用端口预设' },
          { label: '高级选项', text: '反向 DNS 解析（PTR 记录）、超时和并发数' },
          { label: '计划', text: '配置文件可手动运行，或每 1h / 6h / 12h / 24h / 7d 自动运行' },
          { label: '通知', text: '发现新证书、证书变化或即将过期时发送邮件告警' },
        ]
      },
      {
        title: '结果状态',
        items: [
          { label: '已管理', text: '证书的 SHA-256 指纹与 UCM 清单中的某个证书匹配' },
          { label: '未管理', text: '在网络上找到但不在清单中 — 可纳入管理的候选证书' },
          { label: '错误', text: '无法扫描该端点 — 错误提示可区分拒绝连接、DNS、超时和 TLS/SNI 故障；可单独重试或全部重试' },
          { label: '已变化', text: '端点呈现与上次扫描不同的证书时会被标记，并带有"最后变化"时间戳' },
        ]
      },
    ],
    tips: [
      '使用状态筛选按钮筛选结果：已管理、未管理、错误、已过期、即将过期',
      '将发现的证书导出为 CSV 或 JSON — 导出时应用当前筛选条件',
      '为您的服务器子网安排每日扫描，并启用新证书通知',
    ],
    warnings: [
      '运行扫描和管理配置文件需要管理员权限；子网上限为 1024 个地址（/22）',
    ],
  },
  helpGuides: {
    title: '证书发现',
    content: `
## 概述

证书发现扫描您的网络以查找部署在服务器和端点上的 TLS 证书，并将它们与您管理的 PKI 清单进行匹配。使用它来定位未跟踪的证书、检测变化，并监控 UCM 管控之外即将过期的证书。

## 标签页

### 已发现
扫描找到的所有证书，包含状态、到期时间和端点详情。点击一行可打开详情面板，查看证书信息、Subject Alternative Names 和扫描历史（首次发现、最后发现、最后变化）。

### 配置文件
用于定期扫描的已保存扫描配置 — 目标、端口、计划和通知。

### 历史
过去的扫描运行记录，包含持续时间、扫描的目标、找到的证书以及触发者。

## 快速扫描

无需保存配置文件即可运行临时扫描：

1. 点击**快速扫描**
2. 输入**目标** — 每行一个：主机名、IP、CIDR 子网（\`192.168.1.0/24\`）或 \`host:port\`（\`10.0.0.1:8443\`）
3. 输入**端口** — 逗号分隔的 TCP 端口（如 \`443, 8443, 636\`），或选择常用端口预设
4. 可选调整**高级选项** — 反向 DNS 解析（PTR 记录）、超时、并发数
5. 点击**开始扫描** — 进度通过 WebSocket 实时更新

## 扫描配置文件

配置文件保存目标配置以便重复使用：

- **目标和端口** — 与快速扫描格式相同
- **计划** — 手动，或每 1h / 6h / 12h / 24h / 7d 自动运行
- **通知** — 发现新证书、端点上的证书发生变化或发现的证书即将过期时发送邮件告警

使用**扫描**按需运行配置文件，或让调度器按配置的间隔运行。

## 结果状态

- **已管理** — 证书的 SHA-256 指纹与 UCM 清单中的某个证书匹配
- **未管理** — 在网络上找到但不在清单中 — 可纳入管理的候选证书
- **错误** — 无法扫描该端点；错误列显示提示（拒绝连接、DNS 故障、超时、TLS 握手 / SNI 问题）

### 变化检测
当端点呈现与上次扫描不同的证书时，该变化会被记录（保留之前的指纹，带有**最后变化**时间戳），并可触发通知。

## 筛选与导出

- **状态筛选按钮** — 已管理、未管理、错误、已过期、即将过期
- **配置文件筛选** — 将结果限定为某个扫描配置文件
- **导出** — 将发现的证书下载为 CSV 或 JSON（应用筛选条件）
- **重试** — 重新扫描单个错误目标，或一次性**重试所有错误**
- **解析 DNS** — 对发现的 IP 批量进行反向 DNS 解析

## 限制与安全

- 子网上限为 1024 个地址（相当于 IPv4 /22）；每次配置文件扫描最多 1000 个目标
- 私有 RFC1918 网段和环回地址可以扫描 — 符合 UCM 的本地部署模型；链路本地、组播和保留网段被阻止
- 所有扫描操作都会记录审计日志

## 权限

- **read:certificates** — 查看发现的证书、配置文件和历史
- **admin:system** — 创建/编辑配置文件并运行扫描
- **delete:certificates** — 删除发现的结果

> 💡 为您的服务器子网安排每日扫描并启用新证书通知 — 它能捕获在您 PKI 流程之外部署的证书。
`
  }
}
