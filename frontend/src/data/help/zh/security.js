export default {
  helpContent: {
    title: '安全设置',
    subtitle: '认证与访问策略',
    overview: '配置密码策略、会话管理、频率限制和网络安全。这些设置适用于全系统并影响所有用户账户。',
    sections: [
      {
        title: '私钥加密',
        items: [
          { label: '状态与计数器', text: '显示是否已启用加密，以及已存储私钥中加密与未加密的数量' },
          { label: '启用 / 禁用', text: '使用主密钥文件下的 AES-256 加密所有 CA 和证书私钥——请立即备份密钥文件；或禁用以恢复明文存储' },
          { label: 'UCM_REQUIRE_DB_ENCRYPTION_KEY', text: '可选启用的环境变量：没有显式的数据库加密密钥时拒绝启动' },
          { label: 'UCM_REQUIRE_KEY_ENCRYPTION', text: '可选启用的环境变量：未启用私钥加密时拒绝启动' },
        ]
      },
      {
        title: '密码策略',
        items: [
          { label: '最小长度', text: '所需的最少字符数' },
          { label: '复杂性', text: '要求大写字母、小写字母、数字、特殊字符' },
          { label: '过期', text: '在设定天数后强制更改密码' },
          { label: '历史记录', text: '防止重复使用以前的密码' },
        ]
      },
      {
        title: '会话与访问',
        items: [
          { label: '会话超时', text: '不活动一段时间后自动登出' },
          { label: '频率限制', text: '限制登录尝试次数以防暴力攻击' },
          { label: 'IP 限制', text: '允许或拒绝来自特定 IP 范围的访问' },
          { label: '2FA 强制', text: '要求所有用户使用双因素认证' },
        ]
      },
      {
        title: 'mTLS 认证',
        items: [
          { label: '受信任的 CA', text: '选择签发并验证 mTLS 客户端登录证书的 CA' },
          { label: '要求客户端证书', text: '可选地将 mTLS 设为 Web UI 的强制要求——更改 mTLS 设置需要重启服务' },
        ]
      },
    ],
    tips: [
      '启用频率限制以防止自动化攻击工具',
      '使用 IP 限制将管理访问限制在受信任的网络',
    ],
    warnings: [
      '密码策略设置过于严格可能会影响用户体验',
      '启用 IP 限制前请确保至少有一个管理员能够访问系统',
      '安全敏感设置（会话、锁定、HSTS、公共 URL、密码策略）需要 admin:settings 权限——这些字段对操作员锁定',
    ],
  },
  helpGuides: {
    title: '安全设置',
    content: `
## 概述

影响所有用户账户和访问模式的全系统安全配置。

## 私钥加密

使用 AES-256 加密数据库中存储的所有 CA 和证书私钥，由保存在数据库之外的主密钥文件保护。

- **状态与计数器** — 该部分显示是否已启用加密，以及当前**已加密**与**未加密**的密钥数量
- **启用加密** — 生成主密钥文件并加密所有已存储的私钥。请立即备份密钥文件：没有它，加密的密钥将永久丢失
- **禁用加密** — 将所有私钥解密回明文存储（需要确认）

### 启动强制

未配置加密密钥时，UCM 在启动时记录警告但继续运行。两个**可选启用的环境变量**可将其变为硬性失败：

- \`UCM_REQUIRE_DB_ENCRYPTION_KEY\` — 没有显式的数据库加密密钥时拒绝启动（否则集成密钥会回退到从机器 id 派生的密钥）
- \`UCM_REQUIRE_KEY_ENCRYPTION\` — 未启用私钥加密时拒绝启动

两者均接受 \`1\`/\`true\`/\`yes\`/\`on\`。无效的密钥会被视为致命错误，而不是静默回退到明文。

## 密码策略

### 复杂性要求
- **最小长度** — 8 到 32 个字符
- **要求大写字母** — 至少一个大写字母
- **要求小写字母** — 至少一个小写字母
- **要求数字** — 至少一个数字
- **要求特殊字符** — 至少一个符号

### 密码过期
强制用户在设定天数后更改密码。设为 0 则禁用。

### 密码历史记录
防止重复使用最近 N 个密码。用户不能设置与之前 N 个密码相同的密码。

## 会话管理

### 会话超时
在 N 分钟不活动后自动登出用户。仅适用于 Web UI 会话，不适用于 API 密钥。

### 并发会话
限制每个用户的同时会话数。额外的登录将终止最旧的会话。

## 频率限制

### 登录尝试
限制每个 IP 地址在时间窗口内的失败登录尝试次数。超过限制后，该 IP 将被临时封锁。

### 封锁时长
超过登录尝试限制后 IP 被封锁的时间。

## IP 限制

### 允许列表
仅允许来自指定 IP 或 CIDR 范围的连接。其他所有 IP 将被阻止。

### 拒绝列表
阻止特定 IP 或 CIDR 范围。其他所有 IP 被允许。

> ⚠ 使用 IP 限制时要非常小心。配置错误可能会锁定所有用户，包括管理员。请先用单个 IP 测试。

## 双因素认证

### 强制执行
要求所有用户启用 2FA。未设置 2FA 的用户将在下次登录时收到提示。

### 支持的方式
- **TOTP** — 基于时间的一次性密码（身份验证器应用）
- **WebAuthn** — 硬件安全密钥和生物识别

> 💡 至少为管理员账户强制执行 2FA。考虑在安全敏感的环境中为所有用户强制执行。

## mTLS 认证

允许用户使用客户端证书代替密码登录：

- **受信任的 CA** — 选择签发并验证 mTLS 客户端证书的 CA
- **要求客户端证书** — 可选地将 mTLS 设为 Web UI 的强制要求
- 更改 mTLS 设置需要重启服务

## 所需权限

安全敏感设置——会话、锁定、HSTS、公共 URL 和密码策略——需要 **admin:settings** 权限。对于操作员（仅 write:settings），这些字段显示为锁定；卡片的其余部分仍可正常保存。
`
  }
}
